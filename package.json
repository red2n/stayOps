{
  "name": "flowforge",
  "private": true,
  "version": "0.1.0",
  "description": "FlowForge monorepo",
  "license": "Apache-2.0",
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "lint": "pnpm lint:packages && pnpm lint:knip",
    "lint:packages": "lerna run lint",
    "lint:knip": "knip",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.7",
    "knip": "^5.70.1",
    "lerna": "^7.4.2"
  }
}
